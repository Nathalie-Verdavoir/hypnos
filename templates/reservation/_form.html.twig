{{ form_start(form) }}

<p class="card-text border">
	<small class="text-muted">
		<span id="nuitee">0 nuitée</span>
	</small>
</p>
<p class="card-text border">
	<small class="text-muted">Montant total</small>
</p>
<div id="montant">0 €</div>
<button class="btn m-3">{{ button_label|default('Réserver') }}</button>

{{ form_end(form) }}
