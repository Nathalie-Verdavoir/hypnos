{% extends 'base.html.twig' %}

{% block title %}Edit Photo{% endblock %}

{% block body %}
    <h1>Edit Photo</h1>

    {{ include('photo-hotel/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_photo_chambre_index', {'chambre': app.request.get('chambre')}) }}">Retour à la liste des photos</a>

    {{ include('photo-hotel/_delete_form.html.twig') }}
{% endblock %}
