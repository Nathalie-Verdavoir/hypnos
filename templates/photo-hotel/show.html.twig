{% extends 'base.html.twig' %}

{% block title %}Photo{% endblock %}

{% block body %}
    <h1>Photo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ photo.id }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ photo.lien }}</td>
            </tr>
            <tr>
                <th>Cover</th>
                <td>{{ photo.cover ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_photo_index') }}">Retour à la liste des photos</a>

    <a href="{{ path('app_photo_edit', {'id': photo.id}) }}">Modifier</a>

    {{ include('photo-hotel/_delete_form.html.twig') }}
{% endblock %}
