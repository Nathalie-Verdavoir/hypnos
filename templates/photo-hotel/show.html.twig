{% extends 'base.html.twig' %}

{% block title %}Photo
{% endblock %}

{% block body %}
	<h2>Photo</h2>
	<a class="btn btn-primary iconic" href="{{ path('app_photo_index', {'hotel' : photo.hotel.id}) }}" title="Retour à la liste des photos">
		{% include 'boutons/return.html.twig' %}
	</a>
	{% if app.user.hotel %}
		{% if not photo.cover %}
			<a class="btn btn-primary iconic" href="{{ path('app_photo_edit', {'id': photo.hotel.id,'hotel' : photo.hotel,'cover': photo.id}) }}" title="Choisir comme couverture">
				{% include 'boutons/cover.html.twig' with {'disabled' : false } %}
			</a>
		{% endif %}
		{{ include('photo-hotel/_delete_form.html.twig') }}
	{% endif %}
	<img src="https://res.cloudinary.com/hypnosimages/{{ photo.lien}}" class="photo-show">
{% endblock %}
