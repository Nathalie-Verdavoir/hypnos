{% extends 'base.html.twig' %}

{% block title %}Photo{% endblock %}

{% block body %}
    <h1>Photo</h1>
    <a class="btn btn-primary"  href="{{ path('app_photo_index') }}">Retour à la liste des photos</a>

    <a class="btn btn-primary"  href="{{ path('app_photo_edit', {'id': photo.id}) }}">Modifier</a> 
    {{ include('photo-hotel/_delete_form.html.twig') }}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ photo.id }}</td>
            </tr>
            <tr>
                <img src="{{ asset('/uploads/photos/')  }}{{ photo.lien}}" class="photo-show">
            </tr>
            <tr>
                <th>Cover</th>
                <td>{{ photo.cover ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    

   
{% endblock %}
